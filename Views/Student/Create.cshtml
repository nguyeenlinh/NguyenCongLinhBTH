@model NguyenCongLinhBTH2.Models.Student
@* @{
    string id = @Guid.NewGuid().ToString("N");
    }
    <form asp-action="Create" method="post" class="col-6">
    <input type="hidden" asp-for="StudentID" value=@id>
    <div class="mb-3">
    <label for="std-name" class="form-label">Student Name: </label>
    <input type="text" class="form-control" id="std-name" asp-for="StudentName">
    </div>
    <button type="submit" class="btn btn-succcess"> Submit</button>
    </form> *@
<h1>Tạo mới 1 sinh viên</h1>
<hr>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StudentID" class="control-label"></label>
                <input asp-for="StudentID" class="form-control">
                <span asp-validation-for="StudentID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="StudentName" class="control-label"></label>
                <input asp-for="StudentName" class="form-control">
                <span asp-validation-for="StudentName" class="text-danger"></span>
            </div>
            <br>
            <div class="form-group">
                <input class="btn btn-primary" type="submit" value="Save">
                <a class="btn btn-success" asp-action="Index">Danh sách sinh viên</a>
            </div>
        </form>
    </div>
</div>